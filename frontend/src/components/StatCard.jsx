import {motion} from 'framer-motion';export default function StatCard({label,value,icon:Icon,sub}){return <motion.div whileHover={{y:-3}} className="card p-5"><div className="flex items-center justify-between"><div className="grid h-11 w-11 place-items-center rounded-2xl bg-mint text-leaf"><Icon size={21}/></div><span className="text-xs font-bold text-leaf">LIVE</span></div><div className="mt-5 text-3xl font-black tracking-tight">{value}</div><div className="mt-1 font-semibold text-slate-600">{label}</div>{sub&&<div className="mt-2 text-xs text-slate-400">{sub}</div>}</motion.div>}
