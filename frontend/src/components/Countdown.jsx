import {useEffect,useState} from 'react';export default function Countdown({expiry}){const [left,setLeft]=useState(Math.max(0,new Date(expiry)-Date.now()));useEffect(()=>{const t=setInterval(()=>setLeft(Math.max(0,new Date(expiry)-Date.now())),1000);return()=>clearInterval(t)},[expiry]);const s=Math.floor(left/1000),h=Math.floor(s/3600),m=Math.floor(s%3600/60),sec=s%60;const urgency=s<3600?'CRITICAL':s<10800?'HIGH':s<21600?'MEDIUM':'LOW';return <div className={`rounded-2xl px-3 py-2 text-sm font-bold ${urgency==='CRITICAL'?'bg-red-50 text-red-600':urgency==='HIGH'?'bg-orange-50 text-orange-600':'bg-mint text-leaf'}`}><div className="text-[10px] tracking-wider opacity-70">FOOD EXPIRES IN</div>{left?`${String(h).padStart(2,'0')}:${String(m).padStart(2,'0')}:${String(sec).padStart(2,'0')}`:'EXPIRED'} · {urgency}</div>}
