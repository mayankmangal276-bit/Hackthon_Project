import {Link,useNavigate} from 'react-router-dom';import {Bell,LogOut,Map,Menu,X} from 'lucide-react';import {useState} from 'react';import {useAuth} from '../context/AuthContext';import Logo from './Logo';
export default function Navbar(){const {user,logout}=useAuth();const [open,setOpen]=useState(false);const nav=useNavigate();const dashboard=user?`/${user.role.toLowerCase()}/dashboard`:'/login';return <header className="sticky top-0 z-50 border-b border-slate-100 bg-white/90 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4"><Link to="/"><Logo/></Link><nav className="hidden items-center gap-6 md:flex"><Link to="/map" className="text-sm font-semibold text-slate-600 hover:text-ink">Live Map</Link><Link to="/impact" className="text-sm font-semibold text-slate-600 hover:text-ink">Impact</Link>{user&&<Link to={dashboard} className="text-sm font-semibold text-slate-600">Dashboard</Link>}</nav><div className="hidden items-center gap-2 md:flex">{user?<><button className="btn btn-soft"><Bell size={17}/>3</button><button onClick={()=>{logout();nav('/')}} className="btn btn-dark"><LogOut size={16}/>Logout</button></>:<Link className="btn btn-dark" to="/login">Get Started</Link>}</div><button className="md:hidden" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div>{open&&<div className="border-t p-4 md:hidden"><Link className="block p-3" to="/map">Live Map</Link><Link className="block p-3" to="/impact">Impact</Link>{user&&<Link className="block p-3" to={dashboard}>Dashboard</Link>}</div>}</header>}
